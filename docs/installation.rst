============
Installation
============

At the command line::

    $ easy_install django-bestzhlocale

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-bestzhlocale
    $ pip install django-bestzhlocale
